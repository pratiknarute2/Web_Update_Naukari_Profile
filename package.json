{
  "name": "playwright",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "npm run test:clean && npm run test:run",
    "test:clean": "rm -rf playwright-reports test-results",
    "test:run": "npx playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.51.1"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^5.1.0",
    "node-fetch": "^3.3.2"
  }
}
